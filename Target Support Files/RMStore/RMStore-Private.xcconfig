#include "RMStore.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/Headers/Private" "${PODS_ROOT}/Headers/Private/RMStore" "${PODS_ROOT}/Headers/Public" ${RMSTORE_HEADER_SEARCH_PATHS}
LIBRARY_SEARCH_PATHS = ${RMSTORE_LIBRARY_SEARCH_PATHS}
OTHER_LDFLAGS = ${RMSTORE_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}
SKIP_INSTALL = YES